import { StyleSheet } from "react-native";

const global = {
   primary: "white",
   background: "#000000",
   accent: "#FFC104",
   danger: "#EF5858",
};

export default styles = StyleSheet.create({
   container: {
      flex: 1,
      alignItems: "center",
      marginTop: 50,
   },
});
